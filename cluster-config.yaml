apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: transcargo-eks-cluster
  region: us-east-2

managedNodeGroups:
  - name: workers
    instanceType: t3.micro
    desiredCapacity: 1